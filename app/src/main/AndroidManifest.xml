<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.boxfox.appjam14application">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login2Activity"></activity>
        <activity android:name=".MainActivity">

        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".SplashActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".RequestActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Login3Activity" />
        <activity android:name=".StoreDetailActivity" />
        <activity android:name=".MyRequestActivity" >

        </activity>
        <activity android:name=".CalculateActivity" />
        <activity android:name=".ConfirmActivity" />
        <activity android:name=".PurchasePleaseActivity"></activity>
    </application>

</manifest>